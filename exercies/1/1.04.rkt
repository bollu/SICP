;this should be pretty obvious. it changes the sign of the operation from + to - if B is negative. This is so that
;if b is (-ve) , a - b => a - (-b) = a + b. 
;That is *FREAKING* bloody expressive. I'm thoroughly impressed that you can change the freaking sign with an
;if condition. I can feel myself falling for scheme :)  
